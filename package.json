{
  "name": "sign-language-recognition",
  "version": "1.0.0",
  "description": "Sign language recognition monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "workspaces": [
    "packages/*"
  ]
}
